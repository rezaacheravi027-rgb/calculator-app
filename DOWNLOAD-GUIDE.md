# 📦 راهنمای استفاده از فایل ZIP دانلود شده

اگه فایل ZIP ماشین حساب رو دانلود کردی، این مراحل رو دنبال کن:

---

## 📥 قدم ۱: دانلود فایل ZIP

1. برو به: https://github.com/rezaacheravi027-rgb/calculator-app
2. دکمه سبز **"Code"** رو بزن (بالای لیست فایل‌ها)
3. **"Download ZIP"** رو انتخاب کن
4. فایل `calculator-app-main.zip` دانلود میشه

---

## 📂 قدم ۲: استخراج فایل ZIP

### در Windows:
1. راست کلیک روی فایل ZIP
2. انتخاب کن: **"Extract All..."** یا **"استخراج همه"**
3. یک پوشه به نام `calculator-app-main` ساخته میشه

### در macOS:
1. دوبار کلیک روی فایل ZIP
2. خودکار استخراج میشه
3. یک پوشه به نام `calculator-app-main` ساخته میشه

### در Linux:
```bash
unzip calculator-app-main.zip
cd calculator-app-main
```

---

## 🚀 قدم ۳: باز کردن ماشین حساب

حالا داخل پوشه `calculator-app-main` این فایل‌ها رو داری:

```
calculator-app-main/
├── index.html          ← این فایل رو باز کن!
├── style.css
├── script.js
├── README.md
└── ...
```

### روش ۱: دوبار کلیک (ساده‌ترین)
1. پوشه `calculator-app-main` رو باز کن
2. فایل **`index.html`** رو پیدا کن
3. **دوبار کلیک** کن روش
4. ماشین حساب تو مرورگر پیش‌فرض باز میشه!

### روش ۲: کشیدن به مرورگر
1. مرورگر (Chrome, Firefox, Safari, Edge) رو باز کن
2. فایل **`index.html`** رو بگیر و بکش داخل مرورگر
3. ماشین حساب باز میشه!

### روش ۳: باز کردن با مرورگر دلخواه
1. راست کلیک روی `index.html`
2. **Open with** یا **باز کردن با** → انتخاب مرورگر
3. مثلاً: Chrome, Firefox, Safari, Edge

---

## ✅ تست کردن

وقتی فایل باز شد، باید یک ماشین حساب زیبا با:
- دکمه‌های اعداد (0-9)
- عملگرها (+، −، ×، ÷)
- توابع پیشرفته (√، x²، xʸ)
- تاریخچه محاسبات سمت راست
- رنگ‌بندی بنفش و تاریک

ببینی.

---

## 🔧 اگه کار نکرد:

### مشکل ۱: صفحه خالی
- مطمئن شو که **همه فایل‌ها** در یک پوشه هستن
- فایل‌های `style.css` و `script.js` باید کنار `index.html` باشن

### مشکل ۲: طراحی خراب
- Refresh کن (F5 یا Cmd+R)
- Cache مرورگر رو پاک کن (Ctrl+Shift+Delete)

### مشکل ۳: JavaScript کار نمی‌کنه
- کنسول مرورگر رو چک کن (F12)
- مطمئن شو فایل `script.js` وجود داره

---

## 📱 استفاده روی موبایل:

### اگه فایل ZIP رو روی گوشی دانلود کردی:

**Android:**
1. یک File Manager نصب کن (مثل Files by Google)
2. فایل ZIP رو پیدا کن (معمولاً تو Downloads)
3. روش بزن و Extract کن
4. فایل `index.html` رو باز کن
5. انتخاب کن: Chrome یا مرورگر دیگه

**iOS:**
1. برو به Files (فایل‌ها)
2. Downloads → فایل ZIP
3. روش بزن تا extract بشه
4. فایل `index.html` رو باز کن
5. تو Safari باز میشه

---

## 💡 نکات مهم:

✅ **نیازی به اینترنت نیست** - بعد از دانلود، آفلاین کار می‌کنه  
✅ **نیازی به نصب نیست** - فقط یک فایل HTML هست  
✅ **امن هست** - فقط HTML, CSS, JavaScript محلی  
✅ **تاریخچه ذخیره میشه** - تو LocalStorage مرورگر  

---

## 🌐 یا راحت‌تر: آنلاین استفاده کن!

اگه نمی‌خوای دانلود کنی، مستقیم اینجا استفاده کن:
```
https://rezaacheravi027-rgb.github.io/calculator-app/
```

همیشه آپدیت و بدون دانلود!

---

## 🆘 کمک بیشتر:

اگه مشکلی داشتی:
- برو به: https://github.com/rezaacheravi027-rgb/calculator-app/issues
- یک Issue جدید باز کن
- مشکلت رو توضیح بده

---

**ساخته شده با ❤️ توسط [@rezaacheravi027-rgb](https://github.com/rezaacheravi027-rgb)**
